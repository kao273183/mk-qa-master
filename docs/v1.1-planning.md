# mk-qa-master v1.1 — Planning Notes

**Status:** Decision-support doc, NOT a locked PRD · **Author:** Jack Kao · **Date filed:** 2026-06-02 · **Successor to:** v1.0.0 (Stability Lock, Theme B from `docs/v0.11-planning.md` §3)

This doc opens after v1.0.0 shipped to PyPI on 2026-06-02. v1.0's stability lock means **every theme considered here lives under a stricter contract**: additive changes only, deprecation cycles for any rename or removal, snapshot tests catch drift.

Same convention as predecessors (`docs/v0.10-planning.md`, `docs/v0.11-planning.md`): planning material, not a contract. Once a theme is selected, it gets its own mini-PRD per the discipline established in `prd-v0.8-api-security.md`, `prd-v0.10-universal-bookend.md`, and `prd-v1.0-stability-lock.md`.

---

## §1 — Where we are after v1.0.0

| Surface | State |
|---|---|
| MCP tool count | **21** (frozen by v1.0 schema snapshot — no drift without `BREAKING_CHANGE_ACK=true`) |
| Plan bookend coverage | **5 of 21 tools (24%)** — frozen |
| OWASP rules | 5 (API1 / API2 / API3 / API5 / API8) — half of API Top 10 |
| Test runners | 7 (pytest / Jest / Cypress / Go / Maestro / Schemathesis / Newman) |
| Cross-host plugins | 4 (Claude Code / Codex / Hermes / OpenClaw) |
| Tests | 319/319 pass |
| Released to PyPI | v1.0.0 (`pip install mk-qa-master==1.0.0`) |
| Deprecation policy | locked in `docs/DEPRECATION-POLICY.md`; ≥ 1 minor of warning before removal in v2.0 |

The v1.0 surface is now **the contract**. Every theme in §2 has to clear a bar v0.x didn't: not just "is it valuable" but "does the value justify expanding the locked surface?"

---

## §2 — Candidate themes (3 active + 1 placeholder)

### Theme G. Edge AI Inference Runner (primary recommendation — promoted from v0.11 §2)

> Add an `edge` runner that drives RTSP-stream + inference-device test scenarios. Same pattern as the existing Maestro runner but for "RTSP source + inference device" instead of "Android device + adb host".

Build-ready spec exists at `mk-qa-master-edge-ai-enhancement.md`. 4 phases:

| Phase | Days | Independently shippable? |
|---|---|---|
| 1 (desktop YOLO) | ~3d | Yes — `QA_RUNNER=edge` works on its own |
| 2 (analyze_stream / generate) | ~2d | Yes — adds 1 MCP tool |
| 3 (remote inference) | ~2d | Yes — `QA_JETSON_HOST` switches backend |
| 4 (resilience + coach) | ~2d | Yes — optimizer signals |
| **Total** | **~9d** | |

**Constraint from v1.0**: each phase that touches the MCP surface (Phase 2 adds `analyze_stream`) is a **minor bump** under the v1.0 contract:
- `analyze_stream` is a new tool → tool count 21 → 22 (additive, minor-version OK)
- `BREAKING_CHANGE_ACK=true` required for snapshot rewrite
- README + SKILL.md + reference/tool-surface.md tool-count refs auto-flagged by v1.0 PR-2's sync test

So Theme G ships as v1.1.0 (Phase 1+2) → v1.2.0 (Phase 3) → v1.3.0 (Phase 4). OR all-phases-in-one as v1.1.0 if the dogfood holds. Either is valid; the v1.0 stability lock just makes the version progression mechanical.

**Risk: medium-high.** Unchanged from v0.11 §2 G:

1. Heavy optional deps (`ultralytics` → torch ~1GB)
2. Hardware-specific paths inherently flaky (TCP timeouts, device discovery)
3. New CI matrix (ffmpeg + mediamtx + baseline YOLO model)
4. Surface bloat now requires explicit minor bumps (v1.0 lock benefit: change is *visible*; downside: change *requires* version cycling)

**Verdict (carries forward from v0.11 §3):** primary recommendation. The build-ready spec, the carrier risk plan, and the career-portfolio narrative all still apply. v1.0's stability lock makes the rollout mechanical (no scrambling to fit it into v1.0's frozen surface).

---

### Theme C. YAML Config UX (carried forward from v0.10 + v0.11)

> Stop the env-var sprawl. 13 env vars → single yaml.

Unchanged from v0.11 §2 C. Still ~3 PRs, ~3–4 days. Backward compat is additive (env vars remain highest-precedence override).

**v1.0 implication**: zero new tools, zero new args on existing tools. This is **patch-friendly** under the v1.0 contract (could even ship as v1.0.1 if the YAML loader is purely runtime config, not surface). Most likely lands as a minor (v1.1 or v1.2) for visibility, not because it's required.

**Verdict:** keep on shortlist for v1.2+. No new urgency since v0.11.

---

### Theme E. OWASP API4 Rate Limit (carried forward)

> Close the gap from 5 of 10 to 6 of 10 OWASP API categories.

Unchanged from v0.10 §2 E / v0.11 §2 E. ~2–3 PRs, medium-high risk (DoS via misuse), consent gate design is the open question.

**v1.0 implication**: `run_api_security_scan`'s `categories` enum currently has 5 values. Adding `"rate_limit"` is **a new enum value**, which per `DEPRECATION-POLICY.md`'s "what counts as breaking" table is **a breaking change** (callers may match exhaustively). Requires `BREAKING_CHANGE_ACK=true` for the snapshot rewrite + migration entry + a minor bump.

That's heavier than v0.x would have been, but the cost is exactly what v1.0 is supposed to surface.

**Verdict:** v1.2+. The breaking-change cost makes it worth bundling with another v1.x surface change rather than shipping alone.

---

### Theme H. (Placeholder — TBD from v1.0 dogfood feedback)

Reserved for whatever the first wave of v1.0 PyPI users surfaces. If users report bugs that suggest a missing tool or a clarification on existing semantics, that becomes Theme H in v1.2+ planning.

For now: empty placeholder. Don't fill it until feedback exists.

---

## §3 — Recommendation: **Theme G as v1.1**

Reasons:

1. **B-then-G ratified in v0.11 §3.** The whole point of holding Theme G for post-v1.0 was so it lands as a v1.1+ minor under the stable contract, not a pre-v1.0 surface scramble. Backing out now means v0.11 §3's planning logic was wrong, which it wasn't.

2. **The spec is build-ready.** Unlike Themes C and E (which have open design questions on backward compat for YAML loader and consent gate design for rate-limit), Theme G's 4 phases each have concrete code, file paths, env vars, and acceptance criteria from `mk-qa-master-edge-ai-enhancement.md`.

3. **v1.0's stability lock makes G's progression visible.** Each phase that touches the MCP surface is a deliberate minor bump with snapshot test pairing. v0.x's "ship and see" loop becomes v1.x's "declare, snapshot, ship". Theme G is the first real test of that loop — better to find friction here than on a smaller theme.

4. **Career-portfolio narrative remains valid.** The "從 0 到 1 建立 Edge AI 測試自動化框架" framing from the spec's last paragraph is the kind of public artifact that benefits from shipping under a stable contract (`pip install "mk-qa-master[edge]"==1.1.0` is a sharper portfolio claim than `==0.11.0`).

**Sub-recommendation: ship Phases 1+2 together as v1.1.0.** Phase 1 alone is "you can run YOLO via pytest" — no MCP tool exposed yet. Phase 2 (`analyze_stream` tool) is what makes G feel like a v1.1.0 release rather than internal plumbing. Phases 3 and 4 land as v1.2.0 and v1.3.0 (or bundled — TBD when the v1.1 PRD opens).

---

## §4 — Anti-patterns (don't slip these into v1.1)

| Don't | Why |
|---|---|
| Skip the snapshot ack for Phase 2's `analyze_stream` addition | The whole v1.0 mechanism exists to flag this; bypassing it defeats the purpose |
| Pin `ultralytics` / torch as required deps | Optional extras only; v1.0 stability promise covers `pip install mk-qa-master==1.x` working on lightweight installs |
| Add `analyze_stream` AND another tool in the same minor | Each new tool is its own minor bump under v1.0 — bundling makes regression isolation harder |
| Remove the `edge` runner mid-v1.x because someone reports it's slow | Removals need the deprecation cycle (≥ 1 minor of warning); cf. DEPRECATION-POLICY.md |
| Drop the snapshot test in v1.0 to ease G's rollout | Cf. the v1.0 stability promise. The friction IS the feature. |

---

## §5 — Process invariants to carry forward (cumulative from v0.10 + v0.11)

All process invariants from `docs/v0.10-planning.md` §5 and `docs/v0.11-planning.md` §5 carry forward verbatim. Additionally, from v0.11 postmortem §9 "what we'd do differently":

10. **CI workflow check for "ack-set + migration-doc-untouched"** — add `.github/workflows/ci.yml` step that fails when `BREAKING_CHANGE_ACK=true` is in env AND `git diff origin/main...HEAD docs/MIGRATION-*.md` shows no changes. Catches the "stale-doc + ack" edge case. v1.1 housekeeping or first G PR.
11. **PR description templates** — `.github/PULL_REQUEST_TEMPLATE/feat-runner.md` for the new-runner pattern; `feat-tool.md` for new-MCP-tool. Ship in v1.1 housekeeping.

---

## §6 — Open decisions (what Jack picks)

1. **Theme G phase bundling** — Phases 1+2 as v1.1 (proposal), all 4 phases as v1.1, or phase-per-minor?
2. **Theme G dependency policy** — confirm `[edge]` optional extras (proposal) vs core deps?
3. **CI cost** — Phase 1's pytest job needs ffmpeg + mediamtx + a YOLO model file (~5 min added per run). Acceptable, or gate behind a label/path filter?
4. **Glama re-scoring** — v1.0's stability gives a re-score window; check Glama after v1.1 ships to confirm the v1.0 → v1.1 progression is detected.
5. **Marketing** — v1.0 was a milestone (announcement-worthy: Dev.to / Reddit / HN); v1.1 with Edge AI is also blog-worthy (the "Edge AI testing framework from 0 to 1" career narrative is concrete now). Bundle into one v1.x retrospective post, or ship two?

---

## §7 — Non-goals for v1.1 (explicitly)

- Cloudflare Turnstile — pruned per v0.10 postmortem §9; carry forward
- Mobile WebView CAPTCHA — still parked per `v0.8-mobile-postmortem.md`
- Replacing MCP with a different protocol — MCP fits; v1.0 promise locked
- Schema changes for run_tests / solve_visual_challenge / analyze_url / auto_generate_tests / run_api_security_scan / qa_plan / verify_plan — these are the 7 most-touched surfaces; v1.x leaves them alone
- Production hosting / multi-user state — out of scope (would require its own multi-doc design cycle)

---

## §8 — When to revisit this doc

After whichever theme ships, this doc gets:

- a **postmortem section** added at the bottom (what was discovered, what we'd do differently)
- the chosen theme's PRD link referenced
- non-chosen themes either re-promoted (if relevant) or pruned (if obsolete) before v1.2 planning

Same convention as `docs/v0.10-planning.md` §8 and `docs/v0.11-planning.md` §8.

---

*End of v1.1 planning notes. Cross-references: `docs/v0.11-planning.md` (predecessor + postmortem of Theme B → v1.0), `docs/prd-v1.0-stability-lock.md` (locked v1.0 PRD), `docs/MIGRATION-0.x-to-1.0.md` (additive change log), `docs/DEPRECATION-POLICY.md` (formal cycle), `mk-qa-master-edge-ai-enhancement.md` (build-ready Edge AI spec for Theme G).*

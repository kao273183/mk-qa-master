# mk-qa-master v0.11 — Planning Notes

**Status:** Decision-support doc, NOT a locked PRD · **Author:** Jack Kao · **Date filed:** 2026-06-02 · **Successor to:** v0.10.0 (Universal Bookend, theme A from v0.10-planning § 3)

This doc opens after v0.10.0 shipped (`pip install mk-qa-master==0.10.0`, PyPI). It captures the v0.11 candidate themes including the **two carried-over themes from v0.10-planning §2** (themes B, E) and a **new Edge AI candidate** sourced from a user-authored build-ready spec.

Same convention as `docs/v0.10-planning.md`: this is planning material, not a contract. Once one theme is selected, that theme gets its own mini-PRD per the discipline established in `prd-v0.8-api-security.md` and `prd-v0.10-universal-bookend.md`.

---

## §1 — Where we are after v0.10.0

| Surface | State |
|---|---|
| MCP tool count | **21** (unchanged from v0.9.5; v0.10 was purely additive) |
| Plan bookend coverage | **5 of 21 tools (24%)** — `run_tests`, `solve_visual_challenge`, `analyze_url`, `auto_generate_tests`, `run_api_security_scan` |
| OWASP rules | 5 (API1 / API2 / API3 / API5 / API8) — half of API Top 10 |
| Test runners | 7 (pytest / Jest / Cypress / Go / Maestro / Schemathesis / Newman) |
| Cross-host plugins | 4 (Claude Code / Codex / Hermes / OpenClaw) |
| Tests | 311/311 pass |
| Released to PyPI | v0.10.0 (`pip install mk-qa-master==0.10.0`) |

The **bookend is now surface-wide**. The next strategic question is whether to:
- Lock the contract (Theme B — v1.0 stability)
- Cover more OWASP rules (Theme E — rate-limit)
- Reduce env-var sprawl (Theme C — YAML config)
- Open a new platform line (Theme G — Edge AI inference runner)

---

## §2 — Candidate themes (4)

### Theme B. v1.0 Stability Pass (promoted from v0.10 §2 / postmortem §9 recommendation)

> Stop iterating. Promise a stable contract.

What "stable" means:

- Lock the 21-tool surface — no name changes, no breaking arg removals
- Lock `report.json`, `plan_verification`, `Finding`, evidence-row shapes
- Document deprecation policy (one major version of warning before removal)
- Migration guide from 0.x → 1.0 (mostly: pin versions, watch for `keywords` changes)
- Stability test in CI (snapshot the schema; CI fails on diff without an explicit `BREAKING_CHANGE_ACK` env)

PR breakdown (3–4 PRs, ~5–6 days, +1 day vs v0.10-planning's estimate now that 5 tools are part of the bookend lock):

  - PR-1: Schema snapshot tests (tool descriptions, finding shapes, plan shape, evidence shapes per tool)
  - PR-2: Deprecation policy doc + migration guide
  - PR-3: README v1.0 section + bump to 1.0.0
  - PR-4 (optional): Deprecate any rough-edged 0.x APIs we want gone before v1

Risk: **low**. The bookend pattern from v0.10 is exactly the kind of shape that benefits from being explicitly frozen.

**Verdict (carries forward from v0.10 postmortem §9):** primary recommendation.

---

### Theme E. OWASP API4 (Rate Limit) — v0.8 deferred category, carried forward

> Close the gap from 5 of 10 to 6 of 10 OWASP API categories.

Unchanged from v0.10-planning §2 E:

- Low-amplitude burst pattern (≤10 req / 5 sec, then back off)
- Extra consent gate: `QA_API_SECURITY_RATE_LIMIT_CONSENT=true` (above the standard consent)
- Default OFF, opt-in per scan via `categories=[..., "rate_limit"]`
- Token-bucket aware: detect 429 / Retry-After and stop immediately

PR breakdown (2–3 PRs):

  - PR-1: `rate_limit` rule + responsible-probing test against the vulnerable fixture
  - PR-2: Stricter consent gate + docs
  - PR-3: README + version bump

Risk: **medium-high**. Most likely rule to be misused; consent gate must be ironclad.

**Verdict:** good v0.11 stretch goal IF Theme B has remaining budget. Otherwise hold for v0.12.

---

### Theme C. YAML Config UX — carried forward

> Stop the env-var sprawl.

13 env vars (unchanged since v0.9.5):

```
QA_RUNNER QA_PROJECT_ROOT QA_LANG QA_ANDROID_HOST QA_TIMEOUT_SECONDS
QA_VISUAL_CHALLENGE_CONSENT QA_VISUAL_CHALLENGE_AUTHORIZED_DOMAINS
  QA_VISUAL_CHALLENGE_TIMEOUT
QA_API_SECURITY_CONSENT QA_API_SECURITY_AUTHORIZED_DOMAINS
QA_PLAN_PERSIST MK_QA_PLANS_DIR MK_QA_REPORT_PATH
```

Single YAML file fixes memorability, version-control, and team-share simultaneously. Backward compat is additive (env vars stay as highest-precedence overrides).

PR breakdown unchanged from v0.10 §2 C: 2–3 PRs, ~3–4 days.

Risk: **medium** (backward compat for current users).

**Verdict:** shortlist for v0.12+. No new urgency since v0.10.

---

### Theme G. Edge AI Inference Runner (NEW — sourced from `mk-qa-master-edge-ai-enhancement.md`)

> Add an `edge` runner that drives RTSP-stream + inference-device test scenarios.

User-authored build-ready spec proposes an Edge AI runner — same pattern as the existing Maestro runner but for "RTSP source + inference device" instead of "Android device + adb host". Desktop mode (local YOLO) works without hardware; Jetson / remote-inference mode adds a network hop. New MCP tool `analyze_stream` mirrors `analyze_url` for video streams.

**Scope (build-ready spec, 4 phases):**

1. **Phase 1 — desktop YOLO**: `EdgeConfig` + RTSP source mgmt (mediamtx + ffmpeg) + `LocalYolo` backend + IoU/latency metrics + `EdgeInferenceRunner`. Working `QA_RUNNER=edge` pytest run against `fixtures/factory.mp4`.
2. **Phase 2 — analyze / generate**: `analyze_stream` MCP tool + Edge `generate_test` template + ground-truth sidecar loader.
3. **Phase 3 — remote inference**: `RemoteHTTP` backend + `QA_JETSON_HOST` health-check + Maestro-style timeout extension.
4. **Phase 4 — resilience + coach**: degradation injection (netem / 斷流 / 壞幀), Edge flake signals in `get_optimization_plan`, Edge knowledge section in `qa-knowledge.md`.

**New surface area**:

- 1 new runner (`edge`, aliased `rtsp`)
- 1 new MCP tool (`analyze_stream`) — total surface 21 → 22
- 1 new domain knowledge section
- 4 new modules under `src/mk_qa_master/edge/`
- New optional extras: `pip install "mk-qa-master[edge]"` (opencv-python, ultralytics, requests)

**Estimated effort (split across phases):**

| Phase | Days | Independently shippable? |
|---|---|---|
| 1 (desktop) | ~3d | Yes — `QA_RUNNER=edge` works on its own |
| 2 (analyze/generate) | ~2d | Yes — extends Phase 1 |
| 3 (remote) | ~2d | Yes — `QA_JETSON_HOST` switches backend |
| 4 (resilience) | ~2d | Yes — optimizer signals |
| **Total** | **~9d** | But ship-per-phase if scope tightens |

**Risk: medium-high.** Specifically:

1. **Heavy optional deps.** `ultralytics` pulls torch (~1GB), `opencv-python` is ~50MB. Optional extras mitigate but core users still discover the package.
2. **Hardware-specific paths inherently flaky.** Maestro runner taught us this — TCP timeouts, device-not-found, "QA_JETSON_HOST" reachability all add real CI burden.
3. **New CI matrix.** Phase 1's pytest run needs ffmpeg + mediamtx binaries + a baseline YOLO model file. Adds ~5min to CI per run; needs a path that skips when extras aren't installed.
4. **Surface bloat before v1.0 lock.** Adding `analyze_stream` (22nd tool) AND a new runner right before a stability pass means v1.0 promises a bigger surface than it would otherwise. Either accept the bigger lock OR sequence Edge AI as **v1.1+** explicitly.

**Strategic tension**: the spec's last paragraph is honest — "正是職務『從 0 到 1 建立 Edge AI 測試自動化框架』的活證據" (this is the live evidence of building an Edge AI testing framework from 0 to 1 for the job position). That's a meaningful personal-development goal that may or may not align with the user-base for whom v0.10 shipped. The themes B / C / E address **existing** mk-qa-master users; theme G opens a **new** user segment (edge inference QA).

**Verdict:** worth doing — the question is **when**. Two viable orderings:

- **G-then-B**: v0.11 = Edge AI (Phases 1-2 = MVP), v0.12 = v1.0 stability lock (now including the Edge surface). Means v1.0 ships with Edge AI promised.
- **B-then-G**: v0.11 = v1.0 stability lock with 21 tools, v0.12 = Edge AI as a 1.x extension. Means v1.0 ships sooner with a tighter surface.

---

## §3 — Recommendation

> **B-then-G**: ship v0.11 as the v1.0 stability lock with the current 21-tool surface, then ship v0.12 as Edge AI Phases 1-2 (or all 4).

Reasoning:

1. **v0.10 just landed — users need a stable target before another expansion.** v0.7 → v0.10 added 4 release lines (v0.7.x captcha, v0.8 API security, v0.9 plan/verify, v0.10 universal bookend). Every step was additive, but the cumulative drift is real. v1.0 with the current surface gives users (and ourselves) a known-good baseline.

2. **Edge AI is meaningfully different — sequence cleanly.** Theme G isn't a refinement of the current product; it's a new vertical. Bundling it into the v1.0 surface lock means a year from now we'd be back-revving the v1.0 spec because the Edge AI surface didn't quite fit. Easier to ship v1.0 with 21 tools + 7 runners + 4 hosts and add Edge AI as v1.1.

3. **Sequencing favors compound learning.** v1.0 stability pass forces us to write the schema-snapshot tests and migration guide that Edge AI will benefit from as it grows in v1.x. The reverse order (Edge AI first) means v1.0 has to retroactively retrofit those tests around the new surface — more work for the same final state.

4. **Carrier risk.** Edge AI's optional-extras chain (`ultralytics` → torch → ~1GB) is the kind of dep that can break PyPI uploads, Docker image sizes, or Glama plugin install. Best to introduce it as v1.x with the v1.0 stability mechanisms already in place to catch regressions.

5. **The career-portfolio motivation in the Edge AI spec is legitimate but separate.** The build-ready spec is excellent — when Edge AI ships, it will be high-quality. Holding it for v0.12 means it ships AFTER mk-qa-master has a v1.0 stability story; that's more credible than "edge AI feature on a pre-v1.0 product".

**Second-choice ordering**: if there's strong pull (e.g., a concrete job interview deadline) for Edge AI sooner, **G-then-B** is also viable — but commit to NOT mixing G into the v1.0 lock decision. Either G ships entirely pre-v1.0 (v0.11), OR entirely post-v1.0 (v0.12+). Don't half-ship G as part of v0.11 AND defer the rest to v1.x — that's the worst of both worlds.

---

## §4 — Anti-patterns (don't slip these into v0.11)

| Don't | Why |
|---|---|
| Lock v1.0 contract WHILE adding new runners | v1.0's value is "I can pin this version"; mixing surface changes into the lock PR undermines that |
| Defer schema-snapshot tests to v1.1 | The whole point of v1.0 is the snapshot; without it the version number is theater |
| Edge AI Phase 1 without Phase 2 | Phase 1 alone is "you can run YOLO via pytest" — no analyze_stream means host LLMs can't actually generate edge tests, so the user-facing story is incomplete |
| Skip the migration guide | v0.x → 1.0 with no migration doc means every user has to grep for what changed |
| Pin `ultralytics` / torch as required deps | Optional extras only; core install stays light |
| Remove env vars (only) — without YAML loader | Same as v0.10 §4 — strict-replacement breaks existing users. C is additive on purpose. |

---

## §5 — Process invariants to carry forward (from v0.10 postmortem §9)

All of v0.10 planning §5 carries forward verbatim. Additionally, from v0.10 postmortem §9 "what we'd do differently":

7. **Tool-count refs sync test** — single 10-line test that asserts every doc's claimed tool count == count in `server.py`. v0.10 postmortem §9 #2. Add to v0.11 PR-1 (would have caught the 19 → 21 drift between v0.9.0 and v0.9.5).
8. **Soft version-pin tests** — replace `startswith("0.11.")` style hardcoded version asserts with `EXPECTED_MAJOR_MINOR` env or "semver-parseable AND ≥ floor" check. v0.10 postmortem §9 #3.
9. **PR description templates** — `.github/PULL_REQUEST_TEMPLATE/feat-bookend.md` for theme-A-style work; mirror for theme B's schema-snapshot work. v0.10 postmortem §9 #4.

---

## §6 — Open decisions (what Jack picks)

1. **Theme ordering** — B-then-G (recommended), G-then-B, or B-only-skip-G-for-v1.x?
2. **Timeline** — strict ("v0.11.0 within 2 weeks") or open-ended?
3. **v1.0 surface size** — exactly 21 tools (current state), or +1 (`analyze_stream` if Edge AI bundled), or some other number?
4. **Edge AI optional-extras gating** — when shipped, should the package list it in keywords + classifiers (visible to PyPI search), or keep it "discoverable only via README"?
5. **README install pin** — v0.10-planning §6 #4 carries forward unanswered. Worth standardizing on `pip install mk-qa-master==X.Y.Z` everywhere in README?

---

## §7 — Non-goals for v0.11 (explicitly)

- Mobile WebView CAPTCHA — still parked per `v0.8-mobile-postmortem.md`
- Production hosting / multi-user state — out of scope
- LLM-driven schema migrations — Webwright-tier abstraction creep
- Replacing MCP with a different protocol — MCP fits
- **Both B and G in one release** — pick one per anti-pattern §4 #1
- Cloudflare Turnstile — pruned in v0.10 postmortem §9; no user demand

---

## §8 — When to revisit this doc

After whichever theme ships, this doc gets:

- a **postmortem section** added at the bottom (what was discovered, what we'd do differently)
- the chosen theme's PRD link referenced
- non-chosen themes either re-promoted (if relevant) or pruned (if obsolete) before v0.12 planning

Same convention as `docs/v0.10-planning.md` §8, which already has its postmortem §9.

---

*End of v0.11 planning notes. Cross-references: `docs/v0.10-planning.md` (predecessor + postmortem of theme A), `docs/prd-v0.10-universal-bookend.md` (locked v0.10 PRD), `mk-qa-master-edge-ai-enhancement.md` (build-ready Edge AI spec sourced from user).*

---

## §9 — Postmortem: Theme B shipped as v1.0.0 (2026-06-02)

**Status:** Closed. v1.0.0 shipped to PyPI on 2026-06-02 via the standard `publish.yml` OIDC flow. The B-then-G ordering ratified in §3 held — Theme G (Edge AI Runner) is now v1.1+ work per `docs/v1.1-planning.md`.

### What landed

| PR | Scope | Tests | Notes |
|---|---|---|---|
| #77 | Schema snapshot + BREAKING_CHANGE_ACK plumbing + tool_surface.json seed | +3 | Drift detector works both ways (no-ack fails, ack rewrites) |
| #78 | Doc tool-count sync + soft semver version-pin | +2 net | Regex negative-lookbehind fix on first run (caught "v0.7 tools" false positive) |
| #79 | MIGRATION + DEPRECATION docs + ack gating + deprecation-warning pairing | +3 | Ack now refuses without both docs present |
| #80 | v1.0.0 release: README stability section + version bump + tag + release | n/a | Description 384/512 chars; floor raised (0,10,0) → (1,0,0) |

4 PRs, ~5 working days actual (matched PRD §8 estimate). 319/319 tests pass on PyPI v1.0.0. **Zero behavior change** between v0.10.0 and v1.0.0 — v1.0 shipped the promise, not new features.

### What worked

1. **PR-1's snapshot test caught itself.** During PR-3, the tightened ack path (require both docs to exist) engaged correctly the first time I tested it locally without the docs — the mechanism was load-bearing within the same release cycle that built it.

2. **Decision §11 #2 (ack gated on paired docs) is the entire point.** Without the gate, `BREAKING_CHANGE_ACK` becomes a single-env-var shortcut that contributors set once and forget. With it, the override is a real review checkpoint.

3. **Soft version-pin replaced an annoying hardcoded test.** v0.10 PR-5 had to manually retarget the `startswith("0.10.")` test. v1.0 PR-2 parameterized it with a floor; routine bumps in v1.x no longer touch this test.

4. **Doc tool-count sync catches the v0.9.0 → v0.9.5 failure mode permanently.** The 19 → 21 drift that escaped CI for months can't recur — a new tool requires a doc edit, and the test fails if the docs say otherwise.

5. **Description-rewrite for v1.0 fit in 384/512 chars on first attempt.** The PyPI Summary regression test (v0.9.5 hotfix) shaped the rewrite from the start; no second pass needed.

### What we'd do differently

1. **CI workflow check for "ack-set + migration-doc-untouched" never landed.** v0.10 postmortem §9 #1 mentioned an ideal `.github/workflows/ci.yml` step that scans `git diff` for migration-doc edits when `BREAKING_CHANGE_ACK=true` is in the env. PR-3's test-side enforcement covers the basic case (both docs exist) but doesn't enforce "migration-doc was edited IN THIS PR". **Lesson:** A `git diff origin/main...HEAD docs/MIGRATION-*.md` step in CI would catch the edge case where someone uses the ack flag but the migration doc is months stale. Add to v1.1+ housekeeping.

2. **PR description templates deferred again.** v0.10 postmortem §9 #4 flagged this as a 20-minute save. v1.0 PRs each had near-identical structure. **Lesson:** Just ship `.github/PULL_REQUEST_TEMPLATE/*.md` in the next housekeeping PR — even 30 minutes upfront is amortized within 2 PRs.

3. **README "Stability promise" section is wordy.** v1.0's README addition was ~50 lines. Could probably be cut in half. **Lesson:** README sections want a sharp 1-paragraph hook + a single CTA link; depth lives in linked docs.

4. **Tool-surface.md header version-pinning still hardcoded.** The file says "Tool Surface Cheatsheet (v1.0.0 — Stable)". Next minor will need a similar edit. **Lesson:** Either drop the version from the header entirely or wire it to read from pyproject. Defer to v1.x.

### Re-evaluation of v0.11 candidate themes

Per §8, non-chosen themes carry forward or get pruned:

- **Theme G (Edge AI Runner)** — **promote to v1.1 primary recommendation**. The B-then-G ordering specifically reserves G for post-v1.0. Build-ready spec from `mk-qa-master-edge-ai-enhancement.md` still applies; first PR of v1.1 ratifies G's mini-PRD.
- **Theme C (YAML Config UX)** — **keep on shortlist for v1.2+**. 13 env vars still hasn't gotten worse. No new user reports.
- **Theme E (OWASP API4 Rate Limit)** — **keep on shortlist for v1.2+**. Same risk-analysis applies; consent-gate design is still the open question.
- **Theme D (Cloudflare Turnstile)** — **remains pruned** per v0.10 postmortem §9.

### v1.1 candidates ranked

1. **Theme G (Edge AI Runner)** — primary recommendation; B-then-G ratification carries forward
2. Theme C (YAML config UX) — secondary, if G ships fast
3. Theme E (OWASP API4 rate limit) — tertiary
4. New theme H (TBD from v1.0 dogfood feedback) — placeholder

`docs/v1.1-planning.md` opens here.
